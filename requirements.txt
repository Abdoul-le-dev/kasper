# Runtime — compatible Python 3.14
httpx>=0.27
polars>=1.0
pyarrow>=15.0
numpy>=1.26
pydantic>=2.0
python-dotenv>=1.0

# Dev / tests
pytest>=8.0

# NOTE : pas de pandas-ta ni pandas. Les indicateurs (SuperTrend, Hull MA,
# ATR, ADX, VWAP, TTM Squeeze) sont codés à la main en Phase 2, en numpy
# et polars purs. Zéro dépendance à numba, compatible Python 3.14+.
