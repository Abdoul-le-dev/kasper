// Source: Hull Moving Average (Alan Hull, 2005)
// TradingView reference: ta.wma() in Pine v5
// https://www.tradingview.com/pine-script-reference/v5/#fun_ta{dot}wma
//
// Definition:
//   HMA(n) = WMA(2 * WMA(src, n/2) - WMA(src, n), sqrt(n))
//
// Pine snippet:
//   //@version=5
//   indicator("Hull MA", overlay=true)
//   length = input.int(21, "Length")
//   n2 = math.round(length / 2)
//   sqn = math.round(math.sqrt(length))
//   hma = ta.wma(2 * ta.wma(close, n2) - ta.wma(close, length), sqn)
//   plot(hma)
